# overrides for local development, not used in CI
services:
  web:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
      x-bake:
        tags:
          - registry.lil.tools/harvardlil/perma-payments:15-e5ceb9b9b81258a61d69f5040e53b45c
        platforms:
          - linux/amd64
          - linux/arm64
      x-hash-paths:
        - web/requirements.txt
